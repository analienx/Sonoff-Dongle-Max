# R60 one-pass HA log collection and offline triage — 2026-09-22

Parent #18 / baseline #19; complements `R60_OWNER_RESTART_20260921.md`. **Exactly one new read-only HA collection was performed at 2026-09-22T07:02:48Z (09:02 Prague). No follow-up HA polls, neighbor probe, firmware or network change.** Collection script: `deploy/r60_single_ha_capture.py`; offline-only parsers: `deploy/r60_analyze_private_capture.py`, `deploy/r60_offline_failure_triage.py`, `deploy/r60_offline_tx_windows.py`.

## Immutable private capture

The 5,643,610-byte `r60_ha_one_pass_20260922T070247Z.tar.gz` is retained **only locally** at `C:\Workspace\.analienx\sonoff-private\` on the authorized Zephyrus laptop. Do not publish, attach or sync the archive: it contains raw Zigbee2MQTT logs, current `database.db`, `state.json`, and device identifiers. The archive also contains an enumerated capture manifest with timestamps, sanitized selected configuration metadata and container details. All **18 discovered `.log` files** were captured without truncation or size-cap exclusion, along with the bounded Docker stdout tail. Completeness applies only to *files retained by HA at collection*: previously rotated/expired logs are unavailable. Docker events for the requested range were empty in this capture; that is not proof no restarts occurred.

## Observations and limitations

- One live Z2M Docker owner: `ghcr.io/zigbee2mqtt/zigbee2mqtt-aarch64:2.14.0-1`, started `2026-09-22T06:12:05Z`, running at capture. Retained log folders contain **10 Z2M sessions** with graceful shutdown signatures; on Sep 22 distinct starts around **06:07, 06:09, 06:12, 06:15 and 08:12 Prague log time**. **The Sep 21 22:25 restart is documented in [#18's RF experiment](https://github.com/analienx/Sonoff-Dongle-Max/issues/18#issuecomment-5767028250) as an intentional 11→5 dBm change**, not evidence of a spontaneous crash. Other stop initiators are not established by this archive alone. Don't build an automatic restart-prevention fix merely from these timestamps.
- Selected current Z2M config: serial adapter `ember`, configured **TX 8 dBm**, log level `info`; `advanced.channel` was unset in the filtered config, so this field alone does NOT establish live channel. Latest configuration is not independently measured NCP effective TX at earlier windows. No keys, PAN, IEEE or tokens in public evidence.
- Database snapshot: **61 Router + 44 EndDevice + 1 Coordinator =106 records**. A router record is not proof of powered/reachable hardware or direct-neighbor occupancy.
- Across 18 retained `.log` files (178,161 lines): **567 route-error log lines**: `MANY_TO_ONE_ROUTE_FAILURE` 371, `SOURCE_ROUTE_FAILURE` 128, `ADDRESS_CONFLICT` 30, `NON_TREE_LINK_FAILURE` 28, `INDIRECT_TRANSACTION_EXPIRY` 10. Counts combine different TX settings, owner epochs and traffic conditions; they are not an A/B test or root-cause proof.
- The 30 address-conflict lines occurred in Sep 21 20:00–22:59 Prague hourly buckets. The 22:25 session's conflict burst followed the **intentional TX experiment/restart**, so do not claim a defective dongle spontaneously restarted or that 5 dBm caused the conflicts. Today's saved hours continue to show route errors. Historical NWK addresses may no longer map to the same IEEE; never automatically unpair devices based only on log target IDs.
- **Initial offline classifier overcount corrected:** 52,148 supposed command-failure matches included benign `"timeout":...` outgoing-command options; this figure is INVALID and must not be used as command losses. Genuine `Publish 'set'` failure lines and logged device failures exist, but retries and logging layers can multiply one transaction; there is no unique attempt denominator. BedroomBulb1/2/3 intentionally unpowered by relay must be excluded from availability regressions.

## Two fixed-duration slices, from the same saved archive (no HA re-query)

| Prague log window | ROUTE_ERROR_* lines | Many-to-one | Source-route | Non-tree | Error-severity lines | Failed `Publish 'set'` log lines |
|---|---:|---:|---:|---:|---:|---:|
| Sep 22 07:20–08:00 (documented 5 dBm session) | 16 | 14 | 2 | 0 | 4 | 2 (07:21:42, 07:56:02) |
| Sep 22 08:20–09:00 (later owner; 8 dBm in collection-time config) | 28 | 23 | 4 | 1 | 8 | 1 (08:20:45) |

Both windows exclude immediate restart startup. They are adjacent but **not traffic-matched or randomized**; later effective NCP TX at window start was not independently read back. The sample is enough to establish continuing route and command problems, **not** to rank RF-power settings, prove a neighbor-table cause or justify a 60-direct-neighbor patch. Existing [#18 corrected longer-window RF notes](https://github.com/analienx/Sonoff-Dongle-Max/issues/18#issuecomment-5771967473) document Hall bulb and socket `set` failures at 5 dBm.

Current 26/26 neighbor occupancy and previous map-failure mechanism remain unverified by stock retained log files; the staged owner-local neighbor extension remains undeployed. Broad #20 firmware-source auditing is **not** the next prerequisite for operating on the documented service failures.

## Decision from completed capture

**Do not run another HA log collection, blanket map, or another TX-power trial as a substitute for a fix.** Hold TX and avoid gratuitous Z2M restarts/overlapping Zigbee operations; distinguish known deliberate maintenance from genuinely unplanned failures. Use this archive and the existing per-device command failures to select one reproducible Hall-bulb or named-socket command/path case, then implement the smallest justified reliability fix with offline tests, rollback and unchanged network identity. Do not flash production firmware or assume changing the Ember neighbor-table constant to 60 is a validated solution.
