# R60 one-pass HA log collection and offline triage — 2026-09-22

Parent #18 / baseline #19; complements `R60_OWNER_RESTART_20260921.md`. **Exactly one new read-only HA collection was performed at 2026-09-22T07:02:48Z (09:02 Prague). No follow-up HA polls, neighbor probe, firmware or network change.** Collection script: `deploy/r60_single_ha_capture.py`; offline-only parsers: `deploy/r60_analyze_private_capture.py`, `deploy/r60_offline_failure_triage.py`.

## Immutable private capture

The 5,643,610-byte `r60_ha_one_pass_20260922T070247Z.tar.gz` is retained **only locally** at `C:\Workspace\.analienx\sonoff-private\` on the authorized Zephyrus laptop. Do not publish, attach or sync the archive: it contains raw Zigbee2MQTT logs, current `database.db`, `state.json`, and device identifiers. The archive also contains an enumerated capture manifest with timestamps, sanitized selected configuration metadata and container details. All **18 discovered `.log` files** were captured without truncation or size-cap exclusion, along with the bounded Docker stdout tail. This proves completeness only relative to *files retained by HA at collection time*: rotated/expired prior logs cannot be recovered from this snapshot. Docker events for the requested range were empty in this capture; do not read that as proof no restarts occurred.

## Observations and bounded interpretation

- One live Z2M Docker owner observed: `ghcr.io/zigbee2mqtt/zigbee2mqtt-aarch64:2.14.0-1`, started `2026-09-22T06:12:05Z`, running at collection. Retained log folders contain **10 Z2M sessions** with several graceful shutdown signatures; on Sep 22 there are distinct starts around **06:07, 06:09, 06:12, 06:15 and 08:12 Prague local log time**. Exact initiating actor and restart cause are not in this capture. Check operator/supervisor maintenance history *before* attributing restarts to the MG24, firmware or random crashes; do not invent a cause.
- Selected current Z2M config: serial adapter `ember`, configured **TX 8 dBm**, log level `info`; `advanced.channel` was unset in the filtered configuration snapshot, so this field alone does NOT establish live RF channel. No key, PAN, IEEE or token copied into public evidence.
- Database snapshot contains **61 Router + 44 EndDevice + 1 Coordinator =106 records**. Router record is not proof of currently powered/reachable hardware, direct-neighbor occupancy or a stable parent.
- Across the 18 retained `.log` files (178,161 timestamped/non-timestamped lines), **567 distinct route-error log lines**, categorized: `MANY_TO_ONE_ROUTE_FAILURE` 371, `SOURCE_ROUTE_FAILURE` 128, `ADDRESS_CONFLICT` 30, `NON_TREE_LINK_FAILURE` 28, `INDIRECT_TRANSACTION_EXPIRY` 10. These are diagnostic *log lines*, not unique failed ZCL commands, rate-normalized A/B tests, or proof that 26 direct neighbors caused them.
- The 30 address-conflict lines occurred in the Sep 21 20:00–22:59 Prague log-hour buckets; the latest Sep 22 captured hours continue to show many-to-one and source-route errors. Present top many-to-one address associations are provisional because NWK addresses may change; one prominent historical source-route address is absent from the current DB. Do not automatically unpair or blame those devices.
- **Correction of initial offline classifier:** 52,148 purported 'command failure' matches were false positives: broad matching on `timeout` also caught benign `"timeout":...` options printed inside ordinary outgoing ZCL command logs. The refined classifier still sees failed ZCL-command and `Failed to ...` messages but may count one failed transaction in multiple layers (herdsman error, converter retry, Z2M ping). It cannot supply a denominator of attempted user-initiated commands or a unique success rate. Intentionally relay-unpowered bedroom bulbs appear in failed-ping logs and must be excluded from real availability regression.
- Saved logs contain only one broadly matched map-related line and no successful bounded coordinator-local neighbor-table read; current 26/26 and map-failure mechanism **remain unverified**. The existing staged extension is not deployed. Firmware stack audit #20 remains independent and is not needed for the immediate stabilization step.

## Executable decision from this evidence

**Stop broad audits and avoid firmware flashing/60-direct-neighbor modification.** First establish a stable, single Z2M owner without overlapping restarts, OTA/firmware updates, pairing, aggressive maps or other parallel Zigbee operations. Isolate repeated intentional/supervisor owner-stop triggers from NCP failure using existing orchestration and this archive; do not restart Z2M again as a 'test'. Exclude bedroom bulbs intentionally cut by relay from availability/ping regressions. Once the owner is stable, a *single bounded, owner-integrated* coordinator-local neighbor snapshot can be deployed only through the separately reviewed reversible runtime gate if still needed for a targeted fix. No additional HA log collection is required to classify the captured history; no causal declaration that increasing 26 to 60 fixes routing is justified by this data.
