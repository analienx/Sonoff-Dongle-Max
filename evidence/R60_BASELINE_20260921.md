# R60 baseline — 2026-09-21 (initial read-only capture, INCOMPLETE)

Parent: #18; first executable task: #19. This report distinguishes **observed production data**, prior reports, and unmeasured hypotheses. No firmware, Zigbee, HA, pairing, TX or network-map operation was performed for this capture.

## Reproduction and raw-data handling

From `ZephyrusG16`, using the canonical host-key-verified Home Assistant read-only skill in `analienx/config`:

```powershell
py -3 C:\Workspace\worktrees\Sonoff-Dongle-Max-r60\deploy\r60_probe.py
py -3 C:\Workspace\repos\config\skills\home-assistant-readonly\ha_readonly.py --errors
```

For a different checkout location, substitute the local `deploy/r60_probe.py` path; load the canonical skill before use. The probe runs exactly one SSH session and local read-only database/configuration/log/Docker metadata reads. It does **not** open the Zigbee serial device, send Zigbee/MQTT requests, run a map, clear counters or change HA. Output is a sanitized aggregate except for local owner-container metadata; do not commit full local helper output containing IEEE addresses, the Zigbee database, credentials or raw logs. Store any raw capture and network identity only in the authorized local environment. A log directory is a session boundary, **not** necessarily proof of an NCP reset.

## Direct production observation at 2026-09-21T20:05:15Z (22:05:15 Europe/Prague)

| Field | Observation | Evidence/limit |
|---|---|---|
| Database roles | 61 Router, 44 EndDevice, 1 Coordinator = 106 total | Z2M database entries, **not** proof 61 physically powered/reachable |
| Z2M owner | One matching Docker container, `zigbee2mqtt-aarch64:2.14.0-1`; start `2026-09-21T18:36:47.849Z`; container restart count 0 | Docker metadata of this instance, not proof of uninterrupted network uptime |
| Adapter / host stack | `ember`; `zigbee-herdsman` **10.9.1** | live config and package version read inside existing container, without serial access |
| TX | `advanced.transmit_power: 11` | current configuration, **not** a readback from the MG24 radio |
| Channel | 11 appears in this session's Z2M startup log; `advanced.channel` unspecified | startup-log evidence; no live RF-channel command was issued |
| NCP binary identity | **Unknown**: retained P009 source/SDK/version/size do not attest a live exact-image hash or profile | requires supported on-device firmware identity readback; do not label live binary P009 from source alone |
| Powered-off exclusions | Bedroom bulbs deliberately relay-powered off | exclude them from router-availability regression denominator; independently audit any other intentional power-off cases |

## Bounded passive log observation (local log timestamps, UTC+02)

| Session | Interval | Route-error breakdown | Other log mentions |
|---|---|---|---|
| Current `2026-09-21.20-36-50` | 20:36:50–22:05:15, ~88m25s | many-to-one 97; source-route 27; non-tree 12; indirect expiry 7; address conflict 12; **155 total** | 13 address-conflict matching lines (overlapping the route-error events); 110 `Failed to` lines; 37 lines with `command` (may overlap read/write/ping); 20 with `ping`; maximum 13 route-error lines in one minute; 49 distinct NWK targets |
| Previous `2026-09-21.20-20-19` | 20:20:20–20:36:47, ~16m27s | many-to-one 14; non-tree 4; indirect expiry 1; **19 total** | 10 `Failed to` lines (7 mention ping) |

**These are unadjusted line counts**, not independent physical failures or attempts. A single fault may generate multiple log lines or share one address-conflict burst. `Failed to` pattern categories overlap. No per-device success denominator or active-success latency was captured. Do not compare these raw Ember counts with TI Z-Stack's logging semantics or infer TX superiority from these samples. The latest Docker start coincides with the latest log-directory boundary; reason for prior owner restart is not yet established. One `reset`-related log mention per session was seen but **does not itself prove an NCP reset**. No map/Mgmt_Lqi/Mgmt_Rtg mention was found in either scanned session; this does not explain an earlier failed map.

## Neighbor saturation and map evidence gap

`docs/TUNING-RESEARCH.md` says 26/26 neighbor occupancy was seen historically. Search of the current dedicated-repo `docs/` and `evidence/` recovered the **statement** but not the original timestamped table dump, entry identifiers, measurement method, or repeated samples. **Original 26/26 occupancy cannot yet be replayed or used to calculate churn.** The pinned resource profile has 26 neighbor entries; this is a stack/profile fact, not an independently verified MG24 silicon limit. Sixty mesh routers must not be conflated with 60 direct coordinator neighbors.

Current stock Z2M/HA read-only skill exposes its database and logs, **not a bounded owner-mediated NCP neighbor-entry readback**. A second EZSP/serial connection would violate the sole-owner invariant. Z2M `networkmap` is a mesh-wide management operation, **not** a safe substitute for one local neighbor snapshot. Therefore no live occupancy, neighbor IEEE/cost/age/churn or completed map result is claimed. #21 must first identify a safe, explicit single-owner read-only hook or diagnose why the existing owner interface cannot support it. A passive/offline historical map error trace is preferable to repeating a disruptive production map. No Mgmt_Lqi/Mgmt_Rtg request was sent in this task.

## Competing explanations and falsification

| Hypothesis | Discriminating evidence needed | Current status |
|---|---|---|
| Coordinator neighbor aging/churn at capacity | Repeated owner-mediated snapshots: 26/26 occupancy, changed IEEE entries, link-cost/age and failures aligned in time; independent controls for stable neighbors | unproven; historic occupancy claim only |
| Routing/source-route maintenance or pathological path | Time-aligned route-error type, source/target IEEE↔NWK before/after, failed/ACKed ZCL attempt, source-route repair and concentrator setting | multiple route classes seen; causality unproven |
| Address allocation/cache conflict | Preserve event-adjacent IEEE↔NWK mappings and rejoin/leave time; distinguish duplicate allocation from stale mapping | 12 route-error entries within one session; mapping history not captured |
| Broadcast, packet, MAC or retry pressure | Non-clearing event-adjacent BUSY, buffer, PHY/MAC, ASH and NWK counters; correlate with successful/failed transactions | unmeasured; reuse #2 read-only owner instrumentation |
| Transport, router nonresponse or management-map UI abort | Timestamped request/response lifecycle for a single bounded coordinator or router query, differentiated timeout/BUSY/UI cancellation | historical map failure not localized; no broad production retry |
| Individually problematic routers or interference | Per-IEEE device/path, traffic-class, LQI and matched normal-operation windows; exclude intentionally powered-off devices | unresolved; do not convert routers or increase power without evidence |

## Predeclared candidate acceptance contract (fixed before any new candidate)

The **initial baseline is not a pass**; collect comparable, non-disruptive, ordinary-use windows with no overlap from OTA/pairing, full maps, RF changes, firmware changes, power cycling or coordinator-owner changes. Use the same 24-hour local clock/daypart window and inventory for baseline and candidate (minimum **24 hours per arm**); if traffic, production topology or measured opportunity counts differ substantially, report separately rather than falsely treating the arms as matched. Preserve log session boundaries, owner uptime, firmware identity, offered command counts, success, latency and per-class failures. A 10–90-minute sample is only diagnostic.

| Metric | Candidate acceptance threshold, measured on a comparable 24-hour normal-traffic window |
|---|---|
| Network preservation | Exactly one existing PAN/extPAN/channel/IEEE/key/network; no forced re-pair, unplanned coordinator reset or network-down event; production flashing still separately gated by #24 |
| Inventory and availability | ≥60 **physically powered and responding** routers plus ≥100 total joined devices; document denominator and excluded intentional power-offs; ≥99.5% availability of continuously powered routers at their established normal reporting/command opportunity, with no unobserved router counted as healthy |
| User-facing ZCL success | Record attempt-level timestamps/IEEE/command/ACK or explicit result; at least 1,000 measured ordinary direct-control attempts network-wide and ≥100 per each of 10 predeclared critical control devices (if insufficient: **inconclusive**, not pass). ≥99.5% aggregate success and ≥99% success for every monitored critical device; no command class silently excluded |
| ZCL latency | For the same successful critical commands, end-to-end P95 ≤2.0 s and P99 ≤5.0 s; compare against matched baseline, and fail a material regression even if the absolute bound passes |
| Route faults | Same Ember route-error classification/log level in both arms; ≤10 logged route-error events per hour across the mesh averaged over the 24-hour candidate, no unexplained ≥10-event one-minute burst, and no temporal association with failed critical commands; correlate with traffic volume |
| Address conflicts and send admission | Zero address-conflict episodes and zero genuine group/broadcast/unicast `SLStatus.BUSY` command failures in candidate observation; zero new packet/buffer/ASH failure bursts; counters observed within their known read/clear epochs |
| Owner and transport | No unexpected Zigbee2MQTT owner restart, NCP reset or network-down; no sustained ASH/queue/packet-pressure counter growth that is absent in the matched baseline |
| Controlled management/map | In isolated validation first: bounded coordinator-neighbor query paginates to its advertised total with no duplicate/missing entries; every router Mgmt_Lqi/Rtg outcome explicitly classified as response/unsupported/timeout/BUSY/cancel; overall graph reaches 100% *classified* devices (not 100% responsive) within 15 min, normal controls remain operational, and cancel completes within 10 s. Production full-map test requires #24 approval and is not authorized here |

If a device has insufficient reporting/commands to establish a statistically useful per-device rate, mark it **unmeasured** rather than infer success from `lastSeen`. Choose the fixed 10 critical-device identities and collect baseline opportunity counts **before** candidate testing. Historical TI data qualify as a comparator only if matched end-to-end outcomes and observation conditions exist; a cross-stack log-count ratio does not.

## Current disposition / ONE next executable action

**INCOMPLETE:** read-only device/session/log baseline captured and repeatable probe added; current direct-neighbor occupancy, entry churn, historical original 26/26 sample, event-level IEEE↔NWK and command-success denominators, exact NCP image identity and failed-map mechanism remain unverified. The issue #19 acceptance gate must not be checked off yet.

**NEXT:** obtain a bounded owner-mediated **coordinator-only neighbor-table snapshot** (or document the exact owner API deficiency and update #21 with the required one-owner read-only instrumentation). Do not run a full-network map, open a second NCP owner, or deploy firmware for this purpose.
