# R60 single live-owner neighbor snapshot: execution scope (2026-09-22)

User explicitly requested use of the existing production SONOFF coordinator and to proceed with the bounded diagnostic. Scope is ONE coordinator-local read-only `ezspNeighborCount()` plus at most 26 `ezspGetNeighbor(i)` through the EXISTING Zigbee2MQTT Ember owner, no second serial connection, network map, deliberate link failure, OTA, pairing, TX/network/security change or firmware flash. A durable Z2M external-extension save is not merely a read; the host policy requires a narrowly typed deterministic privileged helper with startup-owner/version checks, exact source digest, verified no pre-existing extension, one-shot request, and `finally` removal/rollback. Explicit direct user permission does not bypass that safety gate. No raw private coordinator identities or credentials in public repository.

The stock Z2M 2.14.0 `bridge/request/coordinator_check` invokes `controller.coordinatorCheck()` (adapter backup/device inventory), NOT `ezspGetNeighbor`: it is not a substitute for the requested live neighbor-table occupancy snapshot. Stock health_check and a successful HallBulb1 read likewise do not establish on-air MTORR delivery or broken-route recovery. A one-time neighbor read can show CURRENT saturation and entry quality but still cannot prove MTORR propagation or repair; report that limit precisely.

**Deployment status:** NOT DEPLOYED. Do not describe staged JS, GitHub commit, offline mocks or successful ZCL read as a live coordinator-neighbor snapshot. If an exact approved helper cannot pass source/policy/owner/rollback tests, stop before mutating production. If it passes, execute once, retain full private rows only on the trusted laptop, publish only sanitized summary and exact rollback status.
