# R60 2026-09-22 — one-pass evidence decision

Evidence: [`evidence/R60_ONE_PASS_20260922.md`](../evidence/R60_ONE_PASS_20260922.md). One successful read-only HA session collected the retained Z2M logs (18 files), database, state, selected settings and owner metadata. **No second HA collection pass was made or is required to classify this saved history.** Raw private archive stays on authorized laptop; public repository contains only source and sanitized summaries. The coordinator-only neighbor extension remains staged, undeployed.

**Restart attribution correction:** #18 documents the **21 Sep 22:25 Prague restart as explicitly user-directed TX 11→5 dBm experiment**, so its post-startup address-conflict burst is not evidence of an unplanned NCP crash. The archive includes several subsequent graceful owner stops and starts; their initiating causes are not proven by the captured log lines. Do not build an automatic restart-blocking guard on the unsupported assumption that every restart is unexpected. Identify known configuration-change sessions in the existing timeline, avoid overlapping experiments, and keep raw log rotation in all interval counts.

**Existing evidence for action:** 61 Router database entries are not 61 proven active routers or 61 direct neighbors. All retained log files contain 567 `ROUTE_ERROR_*` log lines and documented Z2M publish/set failures, including real Hall-bulb issues noted in #18; deliberately relay-unpowered bedroom bulbs are not reachability regressions. Log text containing `"timeout":...` in normal command options must NOT be counted as command failure. An 8 dBm Z2M configuration was observed in the single latest capture, not an independent effective NCP power readback.

**Same saved archive, post-settling 40-minute windows:** Sep 22 **07:20–08:00 Prague** (documented 5 dBm session): 16 route-error lines, two `Publish 'set'` failure lines, four error-severity lines. Sep 22 **08:20–09:00 Prague** (later session; 8 dBm in collection-time config): 28 route-error lines, one `Publish 'set'` failure line, eight error-severity lines. No denominator of actual attempts, no unique command success metric, different owner epochs and traffic; do **not** declare either TX level superior or infer that 26-neighbor saturation caused any failure. The later 8 dBm effective NCP level at window start was not independently read back.

**ONE next engineering step:** freeze TX-power tuning and gratuitous Z2M restarts; use the saved archive and existing #18 per-device failure evidence to isolate **one reproducible real command/path failure** (Hall bulbs or the named socket) and implement the narrowest justified reliability fix, with offline tests, rollback and unchanged network identity. Park broad #20 stack research and do not flash or patch a standalone `26→60` constant. A direct-neighbor read may later distinguish saturation from routing trouble, but is not a prerequisite for starting the documented command-failure work. Do not perform another HA log collection for this analysis.
