# R60 2026-09-22 — one-pass evidence and minimal execution decision

Evidence: [`evidence/R60_ONE_PASS_20260922.md`](../evidence/R60_ONE_PASS_20260922.md). One read-only HA capture on the authorized laptop contains all then-retained Z2M logs (18 files), database, state and sanitized settings; raw private archive must stay local. **Do not request another general HA collection, extended observation period, TX sweep, broad map or individual-router re-pairing to start implementation.** The existing owner-only neighbor diagnostic remains staged, NOT deployed.

**Primary demonstrated failure:** 371 `MANY_TO_ONE_ROUTE_FAILURE`, 128 `SOURCE_ROUTE_FAILURE` among 567 route-error lines across multiple owners, and real `Publish 'set'` failures (including Hall bulb commands). Silicon Labs defines a many-to-one route failure as a broken **inbound path to the concentrator**, with the preceding relay reporting the failed link; the reported destination is not proof that the coordinator radio or target device itself broke. The former TI coordinator's better real-world service makes the current SONOFF/Ember concentrator path the engineering priority, not 61 individually defective routers. Logs alone do not prove 26-neighbor saturation, a particular failed hop or NCP firmware defect. Do not equate route-error lines with unique lost user commands.

**Do not add a second refresh loop:** pinned `zigbee-herdsman@10.9.1` already initializes the NCP as a **high-RAM concentrator** with `CONCENTRATOR_MIN_TIME=5s`, `MAX_TIME=60s`, `ROUTE_ERROR_THRESHOLD=3` and `DELIVERY_FAILURE_THRESHOLD=1` by default, and enables source-route discovery with `RESCHEDULE`. These defaults are in `src/adapter/ember/adapter/emberAdapter.ts`. Silicon Labs states concentrator-driven many-to-one route refresh is needed, but it is already configured here. Verify the *captured* per-session 'Using stack config' / '[CONCENTRATOR] Started source route discovery' startup evidence offline before alleging it was disabled; stock logs cannot prove that each broadcast was transmitted, admitted and propagated. Additional uncontrolled MTORR broadcasts/threshold lowering can worsen packet admission and must not be the default remedy. Historical threshold1→3 DELIVERY_FAILURE_THRESHOLD A/B regressed; do not repeat. Source: https://docs.silabs.com/zigbee/9.1.0/zigbee-stack-api/source-route and https://github.com/Koenkk/zigbee-herdsman/blob/v10.9.1/src/adapter/ember/adapter/emberAdapter.ts .

**No restart-cause leap:** Sep 21 22:25 Prague stop/start was an explicitly user-directed TX 11→5 dBm experiment; subsequent graceful stops have no established initiator. Snapshot Z2M TX config was 8 dBm, not measured effective NCP power. Exclude intentionally unpowered bedroom bulbs; do not treat `"timeout"` request options as failures. The Sep 22 07:20–08:00 and 08:20–09:00 windows contained respectively 16 versus 28 route errors and two versus one `Publish 'set'` failure lines; different epochs/traffic mean no valid TX ranking.

**ONE next executable path, automated and bounded:** use only the saved archive and pinned driver source to identify whether `ezspSetConcentrator` / `ezspSetSourceRouteDiscoveryMode(RESCHEDULE)` initialization actually succeeded in each captured owner session and whether clear transport/BUSY evidence accompanies subsequent command losses. In the same local script, select a *single* coordinator-side hypothesis; if logs cannot discriminate an on-air failure, emit `INSUFFICIENT_ON_AIR_EVIDENCE` rather than invent a firmware/config fix. For that one candidate, run offline unit/replay tests (single owner, discovery throttling, no repeated broadcast storms, explicit failure/rollback), then one bounded isolated-hardware acceptance run if required. Keep network identity, channel, TX, NVM, device pairings and production firmware unchanged. No custom 26→60 firmware or aggressive refresh is justified by the present evidence. Production deployment requires an exact reversible, separately authorized change; household reliability fallback is the previously working SLZB-06P7 through a planned identity-preserving migration, not parallel coordinators.
