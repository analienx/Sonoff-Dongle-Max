# One-shot live-owner neighbor-table diagnostic — deterministic execution boundary

Authorize only `z2m_r60_neighbor_snapshot` for the existing SONOFF Zigbee2MQTT owner. The host helper must verify a *single* running expected Z2M owner epoch, Ember Z2M 2.14.0/herdsman 10.9.1, the exact approved SHA-256 of `runtime/r60_neighbor_extension.cjs`, the absence of an existing extension with that exact name, and `permit_join=false` before execution. It must use only the existing owner's Zigbee2MQTT MQTT extension save/snapshot/remove interface, not a new serial socket or a broad network management request. It must remove the extension in a `finally` path, check that the on-disk file is absent, fail closed if an owner restart or OTA/map/pairing activity is found, and preserve full result rows only privately on the authorized laptop.

Never infer a result from a queued/published request. Required evidence is one `bridge/response/r60_neighbor` transaction-matched reply, a matching removed-extension response and verified same Docker owner epoch. On any incomplete rollback, STOP with an explicit non-success status; don't restart Zigbee2MQTT or delete files ad hoc. One snapshot measures table occupancy and quality at one instant, not Link Status reception, MTORR broadcast propagation or automatic route recovery. No firmware, TX/channel/security/device-pairing mutation authorized.

Implementation state: independent deterministic helper must pass offline tests and review before any live extension save. This note is not evidence that an extension was installed or a snapshot taken.
